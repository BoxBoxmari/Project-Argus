// Winner configuration from A/B testing
export const DEFAULT_CONFIG = {
  backend: 'mcp', // MCP Chrome as winner backend
  blockResources: true, // Block heavy resources by default
  locale: 'en-US', // Default locale
  headful: false, // Run headless by default
  browserChannel: 'chrome' // Use Chrome browser channel
};
