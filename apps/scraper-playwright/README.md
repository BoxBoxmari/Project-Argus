# @argus/scraper-playwright

Build: `pnpm -C apps/scraper-playwright build`
Run: `pnpm -C apps/scraper-playwright start` or `dev` for TS entry.
Artifacts: `datasets/` and `artifacts/` are local-only and gitignored.