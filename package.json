{
  "private": true,
  "name": "project-argus",
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "build": "pnpm -r --workspace-concurrency=1 build",
    "clean": "rimraf libs/js-core/dist apps/scraper-playwright/dist",
    "tsc": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "rimraf": "^5.0.5",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  }
}
