{
  "private": true,
  "name": "project-argus",
  "packageManager": "pnpm@9.0.0",
  "type": "module",
  "scripts": {
    "postinstall": "pnpm -C apps/scraper-playwright exec playwright install chromium || true",
    "clean": "pnpm -r --workspace-concurrency=1 run clean || true",
    "build": "pnpm -r --workspace-concurrency=1 build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r run test || true",
    "typecheck": "pnpm -r run typecheck || true",
    "argus:smoke": "pnpm -C apps/scraper-playwright build && pnpm -C apps/scraper-playwright smoke:tls && pnpm -C apps/scraper-playwright smoke:maps"
  },
  "devDependencies": {
    "@eslint/js": "^9.8.0",
    "eslint": "^9.8.0",
    "eslint-config-prettier": "^9.0.0",
    "typescript-eslint": "^7.0.1"
  }
}
