{
  "name": "project-argus",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "pnpm -r --workspace-concurrency=1 build",
    "lint": "echo skip",
    "prestart": "pnpm -w run build",
    "test": "echo tests disabled in CI; enable locally with vitest or jest"
  }
}
